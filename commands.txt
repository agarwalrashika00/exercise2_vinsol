1. created repo: exercise2_vinsol
   initialize git: git init
   connect to remote: git remote add origin https://github.com/agarwalrashika00/exercise2_vinsol.git
2. create 2 files: touch file1.txt
   		 : touch file2.txt
   create tag: git tag "1.0.0" -a -m "first release from master"
3. perform changes in file: vim  file1.txt
   create tag: git tag "1.0.1"
4. create file: touch file3.txt
   create tag: git tag "1.1.0"
5. git checkout 1.0.1
   changes in files: vim file1.txt
   git tag 1.0.2
6. git checkout 1.1.0
   vim file3.txt
   git tag "1.1.1" -a -m "next release"
7. push tags to github:
	git push origin 1.0.0
        git push origin 1.0.1
        git push origin 1.1.0
        git push origin 1.0.2
        git push origin 1.1.1


